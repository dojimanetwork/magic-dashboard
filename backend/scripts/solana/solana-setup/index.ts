import { processCreateAnchor } from "./anchorInit";
import { build } from "./build";
import { deploy } from "./deploy";

const SolAnchor = {
  processCreateAnchor,
  build,
  deploy,
};

export default SolAnchor;
